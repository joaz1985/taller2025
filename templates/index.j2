<html>
    <head>
        <title> Pagina ejemplo de plantilla </title>
    </head>
    <body>
        <h1> Esta es una pagina de ejemplo a partir de una plantilla </h1>
        <br>
        <p> El hostname es "{{ ansible_hostname }}"
        <p>La fecha es "{{ ansible_date_time.date }}" y la hora es "{{ ansible_date_time.time }}" 
        <p> La IP es "{{ ansible_all_ipv4_addresses[0] }}" 
    </body>
</html>
